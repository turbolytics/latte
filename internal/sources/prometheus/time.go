package prometheus

import "time"

// TimeBuckets calculates all complete buckets (of duration d)
// from t until now.
func TimeBuckets(start time.Time, end time.Time, d time.Duration) ([]time.Time, error) {
	return nil, nil
}
